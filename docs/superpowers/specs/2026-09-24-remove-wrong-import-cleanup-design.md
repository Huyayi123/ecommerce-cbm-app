# 移除错误导入清理工具

## 目标

完整移除“管理员：清理 9 月 23 日错误导入”临时工具，恢复“采购 / 在途库存”页面的正常界面和加载路径。

## 变更范围

- 从采购/在途库存页面移除清理面板及其引用。
- 移除通过 `cleanup` 查询参数单独显示清理工具的分支。
- 删除只供该工具使用的组件和匹配/备份工具文件。
- 不修改采购记录、库存、海川物流、导入或删除等正常业务流程。
- 不删除或修改数据库中的备份表与现有业务数据。

## 验证

- 搜索确认源码中不存在 `WrongImportCleanupPanel`、`wrongImportCleanup` 或清理专用入口引用。
- 执行项目构建，确保类型检查和前端打包通过。
- 检查 Git 变更只包含清理工具移除及本设计文档。
